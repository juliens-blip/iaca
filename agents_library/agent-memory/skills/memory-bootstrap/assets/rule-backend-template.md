# Backend Rules

- Keep routers thin and move logic into services when needed.
- Update config, schemas, models, routes, and consumers together when a contract changes.
- Preserve auth, rate limiting, and env-driven security defaults.
- Avoid one-off scripts when the behavior should live in a reusable command or service.
