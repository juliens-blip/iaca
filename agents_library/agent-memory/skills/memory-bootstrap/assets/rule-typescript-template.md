# TypeScript Rules

- Prefer strict types and explicit payload shapes.
- Keep API calls behind shared helpers where possible.
- Do not hardcode environment-specific URLs inside components.
- Handle loading, empty, and error states explicitly.
